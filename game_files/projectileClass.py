from settings import DEFAULT_PROJECTILE_IMG
import pygame
import os
from pygame.locals import *
from imageutils import redimensionImage

class projectile:

    def __init__(self, name = "projectile", imagePath = "", ownerEntity = "", spawnCoords = (0, 0), isEnemy = False, dims = [60, 60], speed):


    def 